// When loading from the Expo dev server (localhost:8081), Metro provides
// all necessary polyfills itself. No overrides needed here.
//
// For the production static export (packaged .exe), this file can be
// extended with any polyfills the bundle requires.
